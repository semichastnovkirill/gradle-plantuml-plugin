buildscript {
    repositories {
        mavenLocal() // for installed org-plantuml-plugin
        mavenCentral() // for PlantUML itself
    }
    dependencies {
        classpath 'org.communis.semichastnovkirill.org:org-plantuml-plugin:0.0.1-SNAPSHOT'
    }
}

apply plugin: 'PlantUml'
