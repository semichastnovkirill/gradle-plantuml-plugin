buildscript {
    repositories {
        mavenLocal() // for installed gradle-plantuml-plugin
        mavenCentral() // for PlantUML itself
    }
    dependencies {
        classpath 'org.communis.semichastnovkirill.gradle:gradle-plantuml-plugin:0.0.1-SNAPSHOT'
    }
}

apply plugin: 'PlantUml'
